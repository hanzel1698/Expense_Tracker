#In Dashboard screen
Calendar should be at the top
then below that should be the expense summary cards
then below that should be the recent expenses list
then below that should be the budget overview
then below that should be the category breakdown chart
then below that should be the monthly trend chart
then below that should be the year trend chart

#In Expense List screen
at top should be the search and add button
below that should be the filter options
then below that should be the expense list with by store and by item buttons on top of that
then at bottom should be the expense details modal

#In Expense Details modal
at top should be the expense details
then at bottom should be the edit and delete buttons

#In Add Expense screen
at top should be the add expense form
then at bottom should be the save button

#In Edit Expense screen
at top should be the edit expense form
then at bottom should be the save button

#in budget screen
at top should be the total budget allocation card
below that should be the budget list
when clicking a category budget, it should open the sub category budget list
above the list should be the catgeory specific allocated & unallocated cards which shows up according to the selected category

#in settings screen
at top should be the google drive sync card
below that should be the category management section
below that should be the sub category management section
below that should be the labels management section
